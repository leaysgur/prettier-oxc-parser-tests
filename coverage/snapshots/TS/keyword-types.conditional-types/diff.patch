===================================================================
--- theirs.ts
+++ ours.ts
@@ -1,6 +1,6 @@
 export type UnwrappedResultRow<T> = {
-  [P in keyof T]: T[P] extends Req<infer a>
+  readonly [P in keyof T]?: T[P] extends Req<infer a>
     ? a
     : T[P] extends Opt<infer b>
       ? b
       : // TEST
