===================================================================
--- theirs.ts
+++ ours.ts
@@ -7,9 +7,10 @@
   equals(other: unknown) {
     return (
       other &&
       typeof other === "object" &&
-      #name in other && // <- this is new!
-      this.#name === other.#name
+      #name in
+        (other && // <- this is new!
+          this.#name === other.#name)
     );
   }
 }
