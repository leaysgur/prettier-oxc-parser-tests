===================================================================
--- theirs.jsx
+++ ours.jsx
@@ -18,29 +18,40 @@
   ]}
 />;
 
 <Something>
-  {() => (
-    <SomethingElse>
-      <span />
-    </SomethingElse>
-  )}
+  {() =>
+    (
+      (
+        <SomethingElse>
+          <span />
+        </SomethingElse>
+      )
+    )
+  }
 </Something>;
 
 <Something>
-  {items.map((item) => (
-    <SomethingElse>
-      <span />
-    </SomethingElse>
-  ))}
+  {items.map(
+    (item) =>
+      (
+        (
+          <SomethingElse>
+            <span />
+          </SomethingElse>
+        )
+      ),
+  )}
 </Something>;
 
 <Something>
   {function () {
     return (
-      <SomethingElse>
-        <span />
-      </SomethingElse>
+      (
+        <SomethingElse>
+          <span />
+        </SomethingElse>
+      )
     );
   }}
 </Something>;
 
@@ -113,9 +124,7 @@
 <SuspendyTree>
   <div style={{ height: 200, overflow: "scroll" }}>
     {Array(20)
       .fill()
-      .map((_, i) => (
-        <h2 key={i}>{i + 1}</h2>
-      ))}
+      .map((_, i) => (<h2 key={i}>{i + 1}</h2>))}
   </div>
 </SuspendyTree>;
