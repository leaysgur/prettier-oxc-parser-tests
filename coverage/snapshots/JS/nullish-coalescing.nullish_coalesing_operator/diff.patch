===================================================================
--- theirs.js
+++ ours.js
@@ -3,10 +3,10 @@
 const x = (foo, bar = foo ?? bar) => {};
 
 foo ? (bar ?? foo) : baz;
 
-foo ?? bar ?? baz;
-foo ?? bar ?? baz;
+foo ?? (bar ?? baz);
+(foo ?? bar) ?? baz;
 
 // Mixing ?? and (&& or ||) requires parens
 // It's a syntax error without it.
 (foo ?? baz) || baz;
